/**
 * Represents a single attachment to a message
 */
class Attachment {
  constructor(attachment, name) {
    this.attachment = attachment;
    this.name = name;
  }
}

module.exports = Attachment;
